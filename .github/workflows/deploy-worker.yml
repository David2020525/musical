name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Musical Worker
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build
        
      - name: Check build output
        run: |
          echo "Checking if dist/_worker.js exists..."
          ls -lh dist/
          if [ ! -f "dist/_worker.js" ]; then
            echo "Error: dist/_worker.js not found!"
            exit 1
          fi
          echo "Build output verified!"
        
      - name: Verify Cloudflare API Token
        run: |
          if [ -z "${{ secrets.CLOUDFLARE_API_TOKEN }}" ]; then
            echo "Error: CLOUDFLARE_API_TOKEN secret is not set!"
            exit 1
          fi
          echo "Cloudflare API Token is configured"
        
      - name: Deploy to Cloudflare Workers
        run: |
          echo "Starting deployment to Cloudflare Workers..."
          npx wrangler deploy --verbose 2>&1 | tee deploy.log || {
            echo "Deployment failed. Error log:"
            cat deploy.log
            exit 1
          }
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        
      - name: Verify deployment
        if: always()
        continue-on-error: true
        run: |
          echo "Deployment completed. Checking worker status..."
          npx wrangler deployments list
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
